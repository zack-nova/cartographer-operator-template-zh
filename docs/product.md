# 产品定位

这个模板把一个工作区变成 agent 操作环境，用来把普通仓库转换成可安装的 harness 模板。

核心目标：

- 同时处理本地路径和远程 Git locator
- 把重复、机械的步骤脚本化
- 把高价值的 curation 判断保留给 agent
- 在 `work/output/` 下生成可复用的模板输出
- 在 `work/analysis/`、`work/plans/`、`work/reports/` 下保留审计文件
